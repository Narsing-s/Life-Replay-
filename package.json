{
  "name": "life-replay",
  "private": true,
  "version": "1.0.0",
  "description": "Life Replay — private personal memory atlas and replay platform.",
  "scripts": {
    "dev": "node -r ./feature-loader.js server.js",
    "start": "node -r ./feature-loader.js server.js",
    "preview": "node -r ./feature-loader.js server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check feature-loader.js && node --check feature-routes.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^13.0.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2"
  }
}
