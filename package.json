{
  "name": "life-replay",
  "private": true,
  "version": "1.0.0",
  "description": "Life Replay — private personal memory atlas and replay platform.",
  "scripts": {
    "dev": "node -r ./feature-loader.js server.js",
    "start": "npm run preflight && node -r ./feature-loader.js server.js",
    "preview": "npm run preflight && node -r ./feature-loader.js server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check security.js && node --check feature-loader.js && node --check feature-routes.js && node --check production-services.js && node --check production-routes.js && node --check scripts/preflight.js && node --check scripts/backup.js && node --check scripts/start-production.js && node --check scripts/production-doctor.js && node --check scripts/restore-check.js",
    "preflight": "node scripts/preflight.js",
    "backup": "node scripts/backup.js",
    "doctor": "node scripts/production-doctor.js",
    "restore-check": "node scripts/restore-check.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^13.0.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2"
  }
}
