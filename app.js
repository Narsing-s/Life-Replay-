const seed=[{id:1,date:'2026-01-01',title:'A new beginning',caption:'New year, new energy.',place:'Home',src:'https://images.unsplash.com/photo-1513151233558-d860c5398176?auto=format&fit=crop&w=1200&q=85'},{id:2,date:'2026-02-14',title:'Small moments',caption:'The moments worth keeping.',place:'City',src:'https://images.unsplash.com/photo-1511988617509-a57c8a288659?auto=format&fit=crop&w=1200&q=85'},{id:3,date:'2026-03-21',title:'Out there',caption:'A day that felt different.',place:'Travel',src:'https://images.unsplash.com/photo-1500534623283-312aade485b7?auto=format&fit=crop&w=1200&q=85'}];
let memories=[...seed],mode='home';const $=s=>document.querySelector(s);const fmt=d=>new Date(d+'T00:00:00').toLocaleDateString(undefined,{day:'numeric',month:'short',year:'numeric'});const month=d=>new Date(d+'T00:00:00').toLocaleString(undefined,{month:'long'});const stats=()=>({places:new Set(memories.map(x=>x.place)).size,years:new Set(memories.map(x=>new Date(x.date).getFullYear())).size});
function header(){return `<header class="header"><div class="brand" onclick="mode='home';render()"><span class="mark">✦</span>life replay</div><button class="ghost" onclick="shareStory()">↗ Share</button></header>`}
function render(){mode==='story'?renderStory():renderHome()}
function renderHome(){let grouped={};memories.forEach(m=>(grouped[new Date(m.date).getFullYear()]??=[]).push(m));let s=stats();let timeline=Object.entries(grouped).sort((a,b)=>b[0]-a[0]).map(([y,items])=>`<div class="year"><div class="yearLabel">${y}</div><div class="rail">${items.sort((a,b)=>a.date.localeCompare(b.date)).map(m=>`<article class="memory" onclick="openMemory(${m.id})"><i class="dot"></i><div class="date">${month(m.date).toUpperCase()} · ${new Date(m.date).getDate()}</div><div class="card"><img src="${m.src}" alt="${m.title}"><div class="body"><h3>${m.title}</h3><p>${m.caption}</p><div class="meta"><span>⌖ ${m.place}</span><span>♡</span></div></div></div></article>`).join('')}</div></div>`).join('');if(!timeline)timeline='<div class="empty">Your story starts with one memory.</div>';
$('#app').innerHTML=`<div class="app">${header()}<main><section class="hero"><div class="eyebrow">✦ YOUR LIFE, REIMAGINED</div><h1>Turn your life<br><em>into a story.</em></h1><p>Bring your favorite photos and moments together. Life Replay turns scattered memories into a beautiful timeline you can revisit and share.</p><div class="actions"><button class="primary" onclick="fileInput.click()">＋ Add memories</button><button class="secondary" onclick="mode='story';render()">▶ Replay my year</button></div><input id="fileInput" hidden type="file" accept="image/*" multiple></section><section class="stats"><div><b>${memories.length}</b><span>memories</span></div><div><b>${s.places}</b><span>places</span></div><div><b>${s.years}</b><span>years</span></div><div><b>∞</b><span>stories</span></div></section><section class="sectionHead"><div><div class="eyebrow">YOUR TIMELINE</div><h2>Moments that matter</h2></div><button class="add" onclick="fileInput.click()">＋</button></section><section class="timeline">${timeline}</section><section class="cta"><div class="eyebrow">✦ KEEP GOING</div><h2>There are more stories<br>waiting to be remembered.</h2><button class="primary" onclick="fileInput.click()">Add another memory →</button></section></main></div>`;$('#fileInput').addEventListener('change',handleFiles)}
function renderStory(){let s=stats();$('#app').innerHTML=`<div class="story"><button class="back" onclick="mode='home';render()">← Back to timeline</button><section class="storyIntro"><div class="eyebrow">✦ YOUR REPLAY</div><h1>One year.<br><em>So many moments.</em></h1><p>${memories.length} memories across ${s.places} places. Here's your story so far.</p><button class="primary" onclick="shareStory()">↗ Share story</button></section><section class="storyGrid">${memories.map(m=>`<article class="storyItem"><img src="${m.src}" alt="${m.title}"><div><small>${fmt(m.date)} · ${m.place}</small><h3>${m.title}</h3><p>${m.caption}</p></div></article>`).join('')}</section><footer>life replay · your memories, your story.</footer></div>`}
function handleFiles(e){[...e.target.files].forEach((f,i)=>{if(!f.type.startsWith('image/'))return;memories.unshift({id:Date.now()+i,date:new Date().toISOString().slice(0,10),title:'A moment worth remembering',caption:'Captured and kept.',place:'My memories',src:URL.createObjectURL(f)});});render();toast('Memory added to your replay ✦')}
function openMemory(id){let m=memories.find(x=>x.id===id);$('#app').insertAdjacentHTML('beforeend',`<div class="modal" id="modal" onclick="closeModal()"><div class="modalBox" onclick="event.stopPropagation()"><button class="close" onclick="closeModal()">×</button><img src="${m.src}" alt="${m.title}"><div class="modalContent"><small>${fmt(m.date)} · ${m.place}</small><h2>${m.title}</h2><p>${m.caption}</p><button class="danger" onclick="removeMemory(${id})">Remove memory</button></div></div></div>`)}
function closeModal(){$('#modal')?.remove()}function removeMemory(id){memories=memories.filter(m=>m.id!==id);closeModal();render();toast('Memory removed')}async function shareStory(){let text=`My Life Replay — ${memories.length} memories across ${stats().places} places.`;try{if(navigator.share)await navigator.share({title:'My Life Replay',text});else{await navigator.clipboard.writeText(text);toast('Replay summary copied')}}catch(e){}}function toast(t){let x=document.createElement('div');x.className='toast';x.textContent=t;document.body.appendChild(x);setTimeout(()=>x.remove(),2200)}render();
