# Security controls

The application applies authenticated user scoping to personal memory APIs, supports password changes/resets, verification tokens, device sessions, audit/security events, request IDs, common security headers, and rate limiting on password-reset requests.

Production still requires HTTPS termination, secret management, durable storage, provider-level key management, centralized monitoring, and regular backup/restore/security testing.
